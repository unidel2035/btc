## Dashboard Веб-интерфейс

Полнофункциональный веб-интерфейс для мониторинга и управления торговым ботом.

### 📋 Обзор

Dashboard предоставляет real-time мониторинг работы бота с удобным веб-интерфейсом. Реализован на Express.js с WebSocket для real-time обновлений и простым HTML/CSS/JS фронтендом.

### ✨ Возможности

#### 📈 Dashboard (Главная страница)
- **Текущий баланс и Equity** — отображение текущего баланса и общей стоимости портфеля
- **PnL статистика** — общий и дневной PnL
- **Win Rate** — процент прибыльных сделок
- **Открытые позиции** — список всех активных позиций с PnL
- **График Equity** — динамика изменения стоимости портфеля
- **Последние сигналы** — недавние торговые сигналы

#### 📡 Signals (Сигналы)
- **Real-time лента сигналов** — автоматическое обновление новых сигналов
- **Фильтры** — по типу стратегии (News Momentum, Sentiment Swing, Technical, Event Driven)
- **Фильтры по действию** — BUY, SELL, HOLD
- **Детальная информация** — причина, уверенность, сила сигнала
- **История сигналов** — все сигналы с timestamp

#### 💼 Positions (Позиции)
- **Список открытых позиций** — все активные позиции
- **Real-time обновление цен** — автоматическое обновление текущих цен
- **PnL для каждой позиции** — в валюте и процентах
- **Ручное закрытие позиций** — кнопка закрытия позиции
- **Изменение SL/TP** — редактирование Stop Loss и Take Profit
- **Детальная информация** — цена входа, текущая цена, размер, время открытия

#### 📰 News Feed (Новости)
- **Лента новостей** — последние новости из различных источников
- **Sentiment метки** — POSITIVE, NEGATIVE, NEUTRAL
- **Sentiment score** — числовая оценка настроения
- **Фильтр по sentiment** — выбор по типу настроения
- **Источники** — CoinDesk, CoinTelegraph, The Block, и другие
- **Ссылки на оригинал** — переход к полной новости

#### 📊 Analytics (Аналитика)
- **Performance отчеты**:
  - Общее количество сделок
  - Прибыльные и убыточные сделки
  - Win Rate
  - Средняя прибыль и убыток
  - Profit Factor
  - Sharpe Ratio
  - Максимальная просадка
- **Trade Journal** — полная история сделок с деталями
- **Таблица сделок** — сортировка и фильтрация

#### ⚙️ Settings (Настройки)
- **Риск-менеджмент**:
  - Максимальный размер позиции
  - Максимальное количество позиций
  - Дневной лимит убытков
  - Общий лимит просадки
  - Дефолтные Stop Loss и Take Profit
  - Trailing Stop настройки
- **Конфигурация стратегий**:
  - Включение/отключение стратегий
  - Параметры каждой стратегии
  - Риск на сделку
  - Максимум позиций для стратегии

### 🏗️ Архитектура

```
src/dashboard/
├── server.ts          # Главный сервер (Express + WebSocket)
├── routes.ts          # REST API endpoints
├── websocket.ts       # WebSocket server для real-time обновлений
├── storage.ts         # In-memory хранилище данных
├── demo.ts            # Генератор демо данных
├── types.ts           # TypeScript типы
├── index.ts           # Экспорты модуля
├── public/            # Frontend файлы
│   ├── index.html     # HTML страница (SPA)
│   ├── styles.css     # Стили (responsive)
│   └── app.js         # Frontend логика
└── README.md          # Документация
```

### 🚀 Запуск

```bash
# Запуск dashboard сервера
npm run dashboard

# Или через tsx напрямую
tsx src/dashboard/server.ts

# С настройками через environment variables
DASHBOARD_PORT=3000 DASHBOARD_HOST=0.0.0.0 npm run dashboard
```

Dashboard будет доступен по адресу: `http://localhost:8080`

### 🔌 API Endpoints

#### Metrics
```
GET /api/metrics
```
Возвращает общие метрики: balance, equity, PnL, win rate, количество сделок.

**Response:**
```json
{
  "balance": 10000,
  "equity": 10500,
  "pnl": 500,
  "pnlPercent": 5.0,
  "totalTrades": 10,
  "winRate": 60,
  "openPositions": 2,
  "dailyPnl": 100,
  "dailyPnlPercent": 1.0
}
```

#### Positions
```
GET /api/positions
GET /api/positions/:id
POST /api/positions/:id/close
PATCH /api/positions/:id
```

**GET /api/positions** — список всех открытых позиций

**POST /api/positions/:id/close** — закрыть позицию
```json
{
  "exitPrice": 51000,
  "reason": "Manual close"
}
```

**PATCH /api/positions/:id** — обновить SL/TP
```json
{
  "stopLoss": 49000,
  "takeProfit": 52500
}
```

#### Signals
```
GET /api/signals?limit=50
```
Возвращает список торговых сигналов.

**Response:**
```json
[
  {
    "id": "uuid",
    "type": "NEWS_MOMENTUM",
    "source": "Demo",
    "symbol": "BTC/USDT",
    "action": "BUY",
    "strength": 85,
    "confidence": 0.9,
    "price": 50000,
    "reason": "Strong bullish news",
    "timestamp": "2024-01-01T00:00:00Z"
  }
]
```

#### News
```
GET /api/news?limit=50
```
Возвращает новости с sentiment анализом.

**Response:**
```json
[
  {
    "id": "uuid",
    "title": "Bitcoin Reaches New High",
    "content": "...",
    "source": "CoinDesk",
    "url": "https://...",
    "sentiment": "POSITIVE",
    "sentimentScore": 0.8,
    "publishedAt": "2024-01-01T00:00:00Z",
    "fetchedAt": "2024-01-01T00:00:00Z"
  }
]
```

#### History & Analytics
```
GET /api/equity?limit=100
GET /api/history?limit=50
GET /api/performance
```

**GET /api/equity** — история equity для графика

**GET /api/history** — история закрытых сделок

**GET /api/performance** — статистика производительности

#### Settings
```
GET /api/strategies
GET /api/strategies/:name
PATCH /api/strategies/:name

GET /api/settings/risk
PATCH /api/settings/risk
POST /api/settings
```

### 🔌 WebSocket API

WebSocket endpoint: `ws://localhost:8080/ws`

#### Типы сообщений от сервера:

**metrics** — обновление метрик (каждые 5 секунд)
```json
{
  "type": "metrics",
  "data": { "balance": 10000, ... },
  "timestamp": "2024-01-01T00:00:00Z"
}
```

**position** — обновление/новая позиция
```json
{
  "type": "position",
  "data": { "id": "uuid", "symbol": "BTC/USDT", ... },
  "timestamp": "2024-01-01T00:00:00Z"
}
```

**signal** — новый торговый сигнал
```json
{
  "type": "signal",
  "data": { "id": "uuid", "type": "NEWS_MOMENTUM", ... },
  "timestamp": "2024-01-01T00:00:00Z"
}
```

**price** — обновление цены (каждые 3 секунды для открытых позиций)
```json
{
  "type": "price",
  "data": {
    "symbol": "BTC/USDT",
    "price": 51000,
    "positionId": "uuid"
  },
  "timestamp": "2024-01-01T00:00:00Z"
}
```

**news** — новая новость
```json
{
  "type": "news",
  "data": { "id": "uuid", "title": "...", ... },
  "timestamp": "2024-01-01T00:00:00Z"
}
```

**notification** — системное уведомление
```json
{
  "type": "notification",
  "data": {
    "message": "Position closed",
    "type": "success"
  },
  "timestamp": "2024-01-01T00:00:00Z"
}
```

### 🎨 Frontend

Простой, но функциональный SPA на чистом JavaScript:

- **Responsive дизайн** — адаптируется под мобильные устройства
- **Dark theme** — приятный для глаз темный интерфейс
- **Real-time обновления** — WebSocket для автоматического обновления
- **Chart.js** — интерактивные графики
- **Фильтрация** — удобные фильтры для сигналов и новостей
- **Управление позициями** — редактирование и закрытие через UI

### 🔒 Безопасность

**Текущая реализация (MVP):**
- ❌ Нет аутентификации (только для локального использования)
- ❌ Нет авторизации
- ✅ CORS настроен для любых источников (dev mode)

**Для production рекомендуется:**
- Добавить JWT аутентификацию
- Настроить CORS для конкретных доменов
- Добавить rate limiting
- HTTPS обязателен
- Добавить API keys для внешних интеграций

### 📊 Demo Mode

По умолчанию включен demo mode с генерацией тестовых данных:

- Автоматическое создание начальных позиций
- Генерация сигналов каждые 10 секунд
- Генерация новостей каждые 20 секунд
- Симуляция изменения цен каждые 3 секунды

Отключить demo mode:
```bash
DASHBOARD_DEMO=false npm run dashboard
```

### 🧪 Тестирование

```bash
# Запуск тестов
npm run test:dashboard

# Или напрямую
tsx tests/dashboard/dashboard.test.ts
```

**Тесты покрывают:**
- ✅ Storage инициализация
- ✅ Добавление/обновление/закрытие позиций
- ✅ Добавление сигналов и новостей
- ✅ Расчет метрик и статистики
- ✅ Конфигурация стратегий и риск-менеджмента
- ✅ Trade history и equity history
- ✅ Корректность расчета PnL

### 📦 Зависимости

**Backend:**
- `express` — HTTP server
- `ws` — WebSocket server
- `cors` — CORS middleware
- `dotenv` — Environment variables

**Frontend:**
- Chart.js — графики
- Vanilla JavaScript — без фреймворков

### 🔧 Настройки

**Environment Variables:**

```env
# Server
DASHBOARD_PORT=8080
DASHBOARD_HOST=localhost

# Demo mode
DASHBOARD_DEMO=true
```

### 🎯 Использование с настоящим ботом

Для интеграции с реальным торговым ботом:

1. **Импортируйте модуль dashboard:**
```typescript
import { storage, DashboardWebSocket } from './src/dashboard/index.js';
```

2. **Добавляйте позиции при открытии:**
```typescript
const position = storage.addPosition({
  symbol: 'BTC/USDT',
  side: 'LONG',
  size: 0.1,
  entryPrice: currentPrice,
  currentPrice: currentPrice,
  stopLoss: stopLossPrice,
  takeProfit: takeProfitPrice,
  pnl: 0,
  pnlPercent: 0,
});

// Отправить через WebSocket
dashboardWs.broadcastPosition(position);
```

3. **Обновляйте позиции:**
```typescript
const updated = storage.updatePosition(positionId, {
  currentPrice: newPrice,
  pnl: calculatedPnl,
  pnlPercent: calculatedPnlPercent,
});
```

4. **Добавляйте сигналы:**
```typescript
const signal = storage.addSignal({
  type: 'NEWS_MOMENTUM',
  source: 'Strategy Name',
  symbol: 'BTC/USDT',
  action: 'BUY',
  strength: 85,
  confidence: 0.9,
  price: 50000,
  reason: 'Strong bullish news detected',
});

dashboardWs.broadcastSignal(signal);
```

5. **Добавляйте новости:**
```typescript
const news = storage.addNews({
  title: newsItem.title,
  content: newsItem.content,
  source: newsItem.source,
  url: newsItem.url,
  sentiment: sentimentResult.label,
  sentimentScore: sentimentResult.score,
  publishedAt: newsItem.publishedAt,
});

dashboardWs.broadcastNews(news);
```

### 🚀 Performance

- **Backend**: Express.js с минимальным overhead
- **Storage**: In-memory для быстрого доступа
- **WebSocket**: Эффективная отправка только изменений
- **Frontend**: Легковесный без фреймворков
- **Charts**: Canvas-based для плавной отрисовки

**Рекомендации для production:**
- Использовать Redis для storage
- Добавить database (PostgreSQL) для истории
- Кешировать часто запрашиваемые данные
- Использовать CDN для статических файлов

### 📝 TODO для будущих улучшений

- [ ] Аутентификация и авторизация
- [ ] Сохранение данных в базу
- [ ] Более продвинутые графики (candlestick charts)
- [ ] Экспорт отчетов (PDF, CSV)
- [ ] Email уведомления
- [ ] Telegram bot интеграция для уведомлений
- [ ] Мобильное приложение
- [ ] Multi-user support
- [ ] Темы оформления (light/dark)
- [ ] Настраиваемый dashboard layout

### 🤝 Интеграция с другими модулями

Dashboard автоматически интегрируется с:

- **News Collector** — новости попадают в News Feed
- **Sentiment Analyzer** — sentiment scores отображаются
- **Trading Strategies** — сигналы отображаются в Signals
- **Risk Manager** — настройки доступны в Settings
- **Position Manager** — позиции отображаются и управляются

### 📖 Примеры

См. `examples/dashboard-example.ts` для полного примера использования.

### 🐛 Troubleshooting

**WebSocket не подключается:**
- Проверьте, что сервер запущен
- Проверьте CORS настройки
- Проверьте firewall

**Данные не обновляются:**
- Проверьте WebSocket соединение в DevTools
- Проверьте, что demo mode включен для тестирования

**Ошибки API:**
- Проверьте логи сервера
- Проверьте формат запросов
- Проверьте наличие данных в storage

---

Создано для проекта BTC News Trading Bot 🤖
